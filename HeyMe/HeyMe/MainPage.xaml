<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HeyMe"
             x:Class="HeyMe.MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0" Orientation="Horizontal">
            <Label Text="Email:" VerticalOptions="Center" />
            <Picker ItemsSource="{Binding EmailChoices}" SelectedItem="{Binding SelectedEmail, Mode=TwoWay}" HorizontalOptions="FillAndExpand"/>
        </StackLayout>
        <StackLayout Grid.Row="1" Orientation="Horizontal" HorizontalOptions="Center">
            <Button Text="Send" Clicked="SendButtonClicked"/>
        </StackLayout>
        <Editor x:Name="EmailBodyEditor" Grid.Row="2" Text="{Binding EmailText, Mode=TwoWay}" VerticalOptions="FillAndExpand" BackgroundColor="LightBlue"/>
    </Grid>
        

</ContentPage>
